SIP for Headless Systems
------------------------

This repository contains Debian packaging files for a version of sip that is
used to build Python bindings for libraries that are used on systems without
display hardware.
